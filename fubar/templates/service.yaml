apiVersion: v1
kind: Service
metadata:
  name: {{ include "fubar.fullname" . }}
  labels:
    {{- include "fubar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fubar.selectorLabels" . | nindent 4 }}
